/* Public configuration only. Never place API tokens, phone numbers or emails here.
   After the owner deploys the Cloudflare Worker, set apiBase to its HTTPS URL.
   Until then the signup is visibly disabled and no personal data is submitted. */
window.SACO_ALERTS_CONFIG=Object.freeze({apiBase:""});
